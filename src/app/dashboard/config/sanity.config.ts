import { defineConfig } from 'sanity';

export default defineConfig({
  name: 'default',
  title: 'Portfolio Dashboard',
  projectId: 'w4peuxk7',
  dataset: 'production',
  basePath: '/dashboard',
  schema: {},
});
